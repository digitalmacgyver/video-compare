numpy>=1.24
opencv-python-headless>=4.8
scipy>=1.10
# Calibration GUI only (tp_calibrate.py). Not used by quality_report,
# quality_metrics, tp_measure, or any of the non-interactive pipeline.
matplotlib>=3.7
